help.start()
help(median) # median 이라는 함수 도움말
?median # median 이라는 함수 도움말
example(median) # median 함수의 예시

help.search("xyplot") # xyplot이 들어간 함수를 검색
??xyplot # xyplot이 들어간 함수를 검색

hero.vector = c("Superman", "Betman", "Spiderman")
apropos("vector") # vector가 포함된 변수명 또는 함수명 검색

RSiteSearch("topicmodel") # https://search.r-project.org/ 웹에서 검색
install.packages("sos")
library(sos)
findFn("social network analysis") # 검색 결과를 테이블 형태로 표시
